# Deployment requirements (Streamlit Community Cloud).
# Uses Gemini for chat and local HuggingFace embeddings (no cloud embedding API).

langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-google-genai>=2.0,<2.2
langchain-huggingface>=0.1
langchain-text-splitters>=0.3

faiss-cpu>=1.8
sentence-transformers>=3.0
pypdf>=5.0

# Web search tool (free, no API key)
ddgs>=6.0

# Interface
streamlit>=1.37

# Config
python-dotenv>=1.0
