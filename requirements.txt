# Cloud/deployment requirements (lean — used by Streamlit Community Cloud).
# For LOCAL use with Ollama, also install requirements-local.txt.

langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-google-genai>=2.0
google-generativeai>=0.8
langchain-text-splitters>=0.3

faiss-cpu>=1.8
pypdf>=5.0

# Web search tool (free, no API key)
ddgs>=6.0

# Interface
streamlit>=1.37

# Config
python-dotenv>=1.0
