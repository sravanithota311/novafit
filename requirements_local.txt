# Local development: everything in requirements.txt PLUS the packages needed
# to run the model locally with Ollama and local embeddings.
-r requirements.txt

langchain-ollama>=0.2
langchain-huggingface>=0.1
sentence-transformers>=3.0

# Optional: run the FastAPI service locally
fastapi>=0.110
uvicorn>=0.30